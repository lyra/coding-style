{
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "test": "yarn workspaces run test",
    "format": "prettier -w .",
    "prepare": "husky install"
  },
  "devDependencies": {
    "jest": "^26.6.3",
    "lerna": "^3.22.1",
    "lint-staged": "^10.5.3",
    "prettier": "^2.2.1",
    "typescript": "^4.1.3",
    "husky": "^6.0.0"
  },
  "prettier": "./packages/prettier-config/index.json",
  "lint-staged": {
    "*": "prettier -w -u"
  }
}
