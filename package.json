{
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "test": "yarn workspaces run test",
    "format": "prettier -w ."
  },
  "devDependencies": {
    "husky": "^4.3.0",
    "jest": "^26.6.3",
    "lerna": "^3.22.1",
    "lint-staged": "^10.5.1",
    "prettier": "^2.2.0",
    "typescript": "^4.1.2"
  },
  "prettier": "./packages/prettier-config/index.json",
  "husky": {
    "hooks": {
      "pre-commit": "yarn test && yarn audit && lint-staged"
    }
  },
  "lint-staged": {
    "*": "prettier -w -u"
  }
}
